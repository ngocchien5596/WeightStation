<Window x:Class="StationApp.UI.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:helpers="clr-namespace:StationApp.UI.Helpers"
        Title="&#x110;&#x103;ng nh&#x1EAD;p - H&#x1EC7; th&#x1ED1;ng qu&#x1EA3;n l&#xFD; tr&#x1EA1;m c&#xE2;n"
        Width="768"
        Height="464"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        WindowStyle="None"
        Background="Transparent"
        AllowsTransparency="True"
        MouseLeftButtonDown="Window_MouseLeftButtonDown">

    <Window.Resources>
        <Style x:Key="LoginTextBoxStyle" TargetType="TextBox">
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="#0F172A"/>
            <Setter Property="Background" Value="#F8FAFC"/>
            <Setter Property="BorderBrush" Value="#CBD5E1"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Height" Value="36"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Padding" Value="8,0,32,0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="border" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="8" SnapsToDevicePixels="True">
                            <ScrollViewer x:Name="PART_ContentHost" Focusable="False" Margin="{TemplateBinding Padding}" VerticalAlignment="Center" HorizontalScrollBarVisibility="Hidden" VerticalScrollBarVisibility="Hidden"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True"><Setter TargetName="border" Property="BorderBrush" Value="#94A3B8"/></Trigger>
                            <Trigger Property="IsFocused" Value="True"><Setter TargetName="border" Property="BorderBrush" Value="#2E86C1"/><Setter TargetName="border" Property="BorderThickness" Value="1.5"/><Setter TargetName="border" Property="Background" Value="#FFFFFF"/></Trigger>
                            <Trigger Property="IsEnabled" Value="False"><Setter TargetName="border" Property="Background" Value="#E2E8F0"/><Setter Property="Foreground" Value="#94A3B8"/></Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="LoginPasswordBoxStyle" TargetType="PasswordBox">
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="#0F172A"/>
            <Setter Property="Background" Value="#F8FAFC"/>
            <Setter Property="BorderBrush" Value="#CBD5E1"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Height" Value="36"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Padding" Value="8,0,32,4"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="PasswordBox">
                        <Border x:Name="border" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="8" SnapsToDevicePixels="True">
                            <ScrollViewer x:Name="PART_ContentHost" Focusable="False" Margin="{TemplateBinding Padding}" VerticalAlignment="Center" HorizontalScrollBarVisibility="Hidden" VerticalScrollBarVisibility="Hidden"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True"><Setter TargetName="border" Property="BorderBrush" Value="#94A3B8"/></Trigger>
                            <Trigger Property="IsFocused" Value="True"><Setter TargetName="border" Property="BorderBrush" Value="#2E86C1"/><Setter TargetName="border" Property="BorderThickness" Value="1.5"/><Setter TargetName="border" Property="Background" Value="#FFFFFF"/></Trigger>
                            <Trigger Property="IsEnabled" Value="False"><Setter TargetName="border" Property="Background" Value="#E2E8F0"/></Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="LoginButtonStyle" TargetType="Button">
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Background" Value="#1B2631"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Height" Value="38"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="8" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <MultiTrigger><MultiTrigger.Conditions><Condition Property="IsMouseOver" Value="True"/><Condition Property="IsEnabled" Value="True"/></MultiTrigger.Conditions><Setter TargetName="border" Property="Background" Value="#263545"/></MultiTrigger>
                            <MultiTrigger><MultiTrigger.Conditions><Condition Property="IsPressed" Value="True"/><Condition Property="IsEnabled" Value="True"/></MultiTrigger.Conditions><Setter TargetName="border" Property="Background" Value="#0F1A24"/></MultiTrigger>
                            <Trigger Property="IsEnabled" Value="False"><Setter TargetName="border" Property="Background" Value="#94A3B8"/><Setter Property="Foreground" Value="#CBD5E1"/></Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="LoginCloseButtonStyle" TargetType="Button">
            <Setter Property="Width" Value="32"/>
            <Setter Property="Height" Value="32"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Foreground" Value="#64748B"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="6"><ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/></Border>
                        <ControlTemplate.Triggers><Trigger Property="IsMouseOver" Value="True"><Setter TargetName="border" Property="Background" Value="#F1F5F9"/><Setter Property="Foreground" Value="#1E293B"/></Trigger></ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ShowPasswordToggleStyle" TargetType="ToggleButton">
            <Setter Property="Width" Value="28"/>
            <Setter Property="Height" Value="28"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Foreground" Value="#94A3B8"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ToggleButton">
                        <Border Background="{TemplateBinding Background}">
                            <TextBlock x:Name="icon" Text="&#xE7B3;" FontFamily="Segoe MDL2 Assets" FontSize="14" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="{TemplateBinding Foreground}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True"><Setter TargetName="icon" Property="Text" Value="&#xE052;"/><Setter Property="Foreground" Value="#2E86C1"/></Trigger>
                            <Trigger Property="IsMouseOver" Value="True"><Setter Property="Foreground" Value="#64748B"/></Trigger>
                            <MultiTrigger><MultiTrigger.Conditions><Condition Property="IsMouseOver" Value="True"/><Condition Property="IsChecked" Value="True"/></MultiTrigger.Conditions><Setter Property="Foreground" Value="#21618C"/></MultiTrigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border Margin="13">
        <Border.Effect><DropShadowEffect BlurRadius="26" ShadowDepth="3" Direction="270" Opacity="0.18" Color="#1B2631"/></Border.Effect>
        <Border CornerRadius="10" ClipToBounds="True">
            <Grid>
                <Grid.ColumnDefinitions><ColumnDefinition Width="300"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
                <Border Grid.Column="0">
                    <Border.Background>
                        <LinearGradientBrush StartPoint="0,0" EndPoint="0.3,1">
                            <GradientStop Color="#1B2631" Offset="0"/>
                            <GradientStop Color="#1E3044" Offset="0.5"/>
                            <GradientStop Color="#203B52" Offset="1"/>
                        </LinearGradientBrush>
                    </Border.Background>
                    <Grid Margin="24,30,24,30">
                        <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="*"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
                        <Grid Grid.Row="0">
                            <Grid.ColumnDefinitions><ColumnDefinition Width="Auto"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
                            <Border Grid.Column="0" Width="52" Height="52" CornerRadius="11" Background="#FFFFFF" HorizontalAlignment="Left" Padding="4" VerticalAlignment="Center">
                                <Image Source="/Assets/logo.jpg" Stretch="Uniform" RenderOptions.BitmapScalingMode="HighQuality"/>
                            </Border>
                            <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="10,0,0,0">
                                <TextBlock Text="C&#xD4;NG TY C&#x1ED4; PH&#x1EA6;N" FontSize="9" FontWeight="Medium" Foreground="#7B95AD" FontFamily="Segoe UI"/>
                                <TextBlock Text="XI M&#x102;NG C&#x1EA8;M PH&#x1EA2;" FontSize="15" FontWeight="Bold" Foreground="#FFFFFF" Margin="0,2,0,0" FontFamily="Segoe UI"/>
                            </StackPanel>
                        </Grid>
                        <Border Grid.Row="1" Height="2" Background="#C5A059" HorizontalAlignment="Left" Width="36" Margin="0,18,0,0" CornerRadius="1"/>
                        <StackPanel Grid.Row="2" VerticalAlignment="Top" Margin="0,18,0,0">
                            <TextBlock FontSize="22" FontWeight="Bold" Foreground="#FFFFFF" FontFamily="Segoe UI" TextWrapping="Wrap" LineHeight="28">
                                <Run Text="H&#x1EC7; th&#x1ED1;ng"/>
                                <LineBreak/>
                                <Run Text="qu&#x1EA3;n l&#xFD; tr&#x1EA1;m c&#xE2;n"/>
                            </TextBlock>
                        </StackPanel>
                        <StackPanel Grid.Row="3">
                            <TextBlock Text="Copyright &#xA9; 2026 by CNTT" FontSize="11" Foreground="#94B3CF" FontFamily="Segoe UI"/>
                            <TextBlock Text="{Binding VersionText}" FontSize="11" Foreground="#94B3CF" Margin="0,3,0,0" FontFamily="Segoe UI"/>
                        </StackPanel>
                    </Grid>
                </Border>
                <Border Grid.Column="1" Background="#FFFFFF">
                    <Grid>
                        <Button Style="{StaticResource LoginCloseButtonStyle}" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="0,16,16,0" Click="CloseButton_Click" Content="&#x2715;"/>
                        <StackPanel VerticalAlignment="Center" Margin="36,0" MaxWidth="300">
                            <TextBlock Text="&#x110;&#x103;ng nh&#x1EAD;p" FontSize="20" FontWeight="Bold" Foreground="#1E293B" FontFamily="Segoe UI"/>
                            <Border Margin="0,18,0,0" Padding="12,10" CornerRadius="8" Background="#FEF2F2" BorderBrush="#FECACA" BorderThickness="1" Visibility="{Binding ErrorMessage, Converter={StaticResource StringToVis}}">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="&#xEA39;" FontFamily="Segoe MDL2 Assets" FontSize="13" Foreground="#DC2626" VerticalAlignment="Center" Margin="0,0,8,0"/>
                                    <TextBlock Text="{Binding ErrorMessage}" Foreground="#DC2626" FontSize="12" TextWrapping="Wrap" VerticalAlignment="Center" FontFamily="Segoe UI"/>
                                </StackPanel>
                            </Border>
                            <StackPanel Margin="0,18,0,0">
                                <TextBlock Text="T&#xEA;n &#x111;&#x103;ng nh&#x1EAD;p" FontSize="12" FontWeight="SemiBold" Foreground="#334155" FontFamily="Segoe UI" Margin="0,0,0,6"/>
                                <Grid>
                                    <TextBox x:Name="UsernameTextBox" Style="{StaticResource LoginTextBoxStyle}" Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}" IsEnabled="{Binding IsInputEnabled}"/>
                                    <TextBlock Text="&#xE77B;" FontFamily="Segoe MDL2 Assets" FontSize="14" Foreground="#94A3B8" VerticalAlignment="Center" HorizontalAlignment="Right" Margin="0,0,12,0" IsHitTestVisible="False"/>
                                </Grid>
                            </StackPanel>
                            <StackPanel Margin="0,12,0,0">
                                <TextBlock Text="M&#x1EAD;t kh&#x1EA9;u" FontSize="12" FontWeight="SemiBold" Foreground="#334155" FontFamily="Segoe UI" Margin="0,0,0,6"/>
                                <Grid>
                                    <TextBox Style="{StaticResource LoginTextBoxStyle}" Text="{Binding Password, UpdateSourceTrigger=PropertyChanged}" IsEnabled="{Binding IsInputEnabled}" Visibility="{Binding IsPasswordVisible, Converter={StaticResource BoolToVis}}"/>
                                    <PasswordBox x:Name="PasswordBoxInput" Style="{StaticResource LoginPasswordBoxStyle}" IsEnabled="{Binding IsInputEnabled}" helpers:PasswordBoxAssistant.BindPassword="True" helpers:PasswordBoxAssistant.BoundPassword="{Binding Password, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Visibility="{Binding IsPasswordVisible, Converter={StaticResource InverseBoolToVis}}"/>
                                    <ToggleButton Style="{StaticResource ShowPasswordToggleStyle}" IsChecked="{Binding IsPasswordVisible}" HorizontalAlignment="Right" Margin="0,0,6,0" VerticalAlignment="Center"/>
                                </Grid>
                            </StackPanel>
                            <Button Margin="0,20,0,0" Style="{StaticResource LoginButtonStyle}" Command="{Binding LoginCommand}" IsDefault="True">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="{Binding LoginButtonText}" VerticalAlignment="Center" FontFamily="Segoe UI"/>
                                    <TextBlock Text="&#xE72A;" FontFamily="Segoe MDL2 Assets" FontSize="12" VerticalAlignment="Center" Margin="8,0,0,0"/>
                                </StackPanel>
                            </Button>
                        </StackPanel>
                    </Grid>
                </Border>
            </Grid>
        </Border>
    </Border>
</Window>
