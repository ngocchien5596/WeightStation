<UserControl x:Class="StationApp.UI.Views.OutgoingVehicleListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="Transparent">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/StationApp.UI;component/Themes/AppTheme.xaml"/>
                <ResourceDictionary Source="/StationApp.UI;component/Styles/ButtonStyles.xaml"/>
                <ResourceDictionary Source="/StationApp.UI;component/Styles/InputStyles.xaml"/>
                <ResourceDictionary Source="/StationApp.UI;component/Styles/BasePageStyles.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>
    <Grid Background="#FFFFFF">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Style="{StaticResource HeaderBorder}">
            <DockPanel>
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="{StaticResource Outgoing.Label.SessionNo}" Style="{StaticResource HeaderLabel}" Margin="0,0,8,0"/>
                    <TextBox Width="168"
                             Style="{StaticResource StandardTextBoxStyle}"
                             Text="{Binding SearchSessionNo, UpdateSourceTrigger=PropertyChanged}">
                        <TextBox.InputBindings>
                            <KeyBinding Key="Enter" Command="{Binding RefreshCommand}"/>
                        </TextBox.InputBindings>
                    </TextBox>

                    <TextBlock Text="{StaticResource Workflow.Label.VehiclePlate}" Style="{StaticResource HeaderLabel}" Margin="20,0,8,0"/>
                    <TextBox Width="168"
                             Style="{StaticResource StandardTextBoxStyle}"
                             Text="{Binding SearchVehiclePlate, UpdateSourceTrigger=PropertyChanged}">
                        <TextBox.InputBindings>
                            <KeyBinding Key="Enter" Command="{Binding RefreshCommand}"/>
                        </TextBox.InputBindings>
                    </TextBox>

                    <Button Command="{Binding RefreshCommand}" Style="{StaticResource HeaderActionButton}" Margin="20,0,0,0">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="&#xE72C;" FontFamily="Segoe MDL2 Assets" FontSize="13" VerticalAlignment="Center" Margin="0,0,6,0"/>
                            <TextBlock Text="{StaticResource Workflow.RefreshButton}" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Button>
                </StackPanel>
            </DockPanel>
        </Border>

        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,8,0,12">
            <Button Content="XEM CHI TIẾT" Command="{Binding ShowDetailsCommand}" Style="{StaticResource WorkflowButtonStyle}" Margin="0,0,6,0"/>
            <Button Content="IN PC" Command="{Binding PrintWeighTicketCommand}" Style="{StaticResource UtilityButtonStyle}" Margin="0,0,6,0"/>
            <Button Content="IN PGN" Command="{Binding PrintDeliveryTicketCommand}" Style="{StaticResource UtilityButtonStyle}"/>
        </StackPanel>

        <Border Grid.Row="2" Margin="12,0,12,12">
            <DataGrid ItemsSource="{Binding Vehicles}" SelectedItem="{Binding SelectedVehicle}"
                      Style="{StaticResource SystemDataGridStyle}"
                      ColumnHeaderStyle="{StaticResource SystemDataGridColumnHeaderStyle}"
                      RowStyle="{StaticResource SystemDataGridRowStyle}"
                      CellStyle="{StaticResource SystemDataGridCellStyle}">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="{StaticResource Outgoing.Label.SessionNo}" Binding="{Binding SessionNo}" Width="140" ElementStyle="{StaticResource GridTextCenter}"/>
                    <DataGridTextColumn Header="{StaticResource Outgoing.Column.TransactionType}" Binding="{Binding TransactionType, Converter={StaticResource TransactionTypeDisplayConverter}}" Width="100" ElementStyle="{StaticResource GridTextCenter}"/>
                    <DataGridTextColumn Header="{StaticResource Outgoing.Column.VehiclePlate}" Binding="{Binding VehiclePlate}" Width="120" ElementStyle="{StaticResource GridTextCenter}"/>
                    <DataGridTextColumn Header="{StaticResource Outgoing.Column.Mooc}" Binding="{Binding MoocNumber}" Width="110" ElementStyle="{StaticResource GridTextCenter}"/>
                    <DataGridTextColumn Header="{StaticResource Outgoing.Column.DriverName}" Binding="{Binding DriverName}" Width="150" ElementStyle="{StaticResource GridTextLeft}"/>
                    <DataGridTextColumn Header="NET (KG)" Binding="{Binding NetWeight, StringFormat=N0}" Width="120" ElementStyle="{StaticResource GridTextRight}"/>
                    <DataGridTextColumn Header="SỐ DÒNG" Binding="{Binding LineCount}" Width="90" ElementStyle="{StaticResource GridTextCenter}"/>
                    <DataGridCheckBoxColumn Header="IN PC" Binding="{Binding HasPrintedMasterWeighTicket, Mode=OneWay}" Width="80"/>
                    <DataGridCheckBoxColumn Header="IN PGN" Binding="{Binding AllDeliveryTicketsPrinted, Mode=OneWay}" Width="80"/>
                    <DataGridTextColumn Header="{StaticResource Outgoing.Column.CompletedAt}" Binding="{Binding CompletedAt, StringFormat='dd/MM/yyyy HH:mm'}" Width="150" ElementStyle="{StaticResource GridTextCenter}"/>
                </DataGrid.Columns>
            </DataGrid>
        </Border>

        <Grid Background="#AA000000" Visibility="{Binding IsDetailsVisible, Converter={StaticResource BoolToVis}}" Grid.RowSpan="3">
            <Border Background="White" CornerRadius="8" Padding="24" Width="980" MaxHeight="720" VerticalAlignment="Center">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <TextBlock Text="CHI TIẾT LƯỢT XE RA" FontSize="18" FontWeight="Bold" Margin="0,0,0,16" Foreground="#2C3E50"/>

                    <DataGrid Grid.Row="1"
                              ItemsSource="{Binding DetailLines}"
                              AutoGenerateColumns="False"
                              IsReadOnly="True"
                              Style="{StaticResource SystemDataGridStyle}"
                              ColumnHeaderStyle="{StaticResource SystemDataGridColumnHeaderStyle}"
                              RowStyle="{StaticResource SystemDataGridRowStyle}"
                              CellStyle="{StaticResource SystemDataGridCellStyle}">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="STT" Binding="{Binding SequenceNo}" Width="50"/>
                            <DataGridTextColumn Header="MÃ ĐKPT" Binding="{Binding ErpVehicleRegistrationId}" Width="130"/>
                            <DataGridTextColumn Header="KHÁCH HÀNG" Binding="{Binding CustomerName}" Width="240"/>
                            <DataGridTextColumn Header="SẢN PHẨM" Binding="{Binding ProductName}" Width="220"/>
                            <DataGridTextColumn Header="PL (KG)" Binding="{Binding PlannedWeight, StringFormat=N0}" Width="100"/>
                            <DataGridTextColumn Header="BAO PL" Binding="{Binding PlannedBagCount}" Width="80"/>
                            <DataGridTextColumn Header="THỰC (KG)" Binding="{Binding ActualAllocatedWeight, StringFormat=N0}" Width="110"/>
                            <DataGridTextColumn Header="BAO THỰC" Binding="{Binding ActualAllocatedBagCount}" Width="90"/>
                        </DataGrid.Columns>
                    </DataGrid>

                    <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
                        <Button Content="Đóng" Command="{Binding CloseDetailsCommand}" Style="{StaticResource UtilityButtonStyle}"/>
                    </StackPanel>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</UserControl>
